GITLAB_URL=https://gitlab.example.com
GITLAB_PRIVATE_TOKEN=
# Opcional: path do grupo no GitLab (ex: web-sites-react). Se definido, os projetos são listados via API do grupo em vez de projects.json
GITLAB_GROUP_PATH=
